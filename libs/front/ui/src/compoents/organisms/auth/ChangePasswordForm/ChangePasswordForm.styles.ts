import { makeStyles } from '../../../../theme/makeStyles';

const useStyles = makeStyles({ name: 'ChangePassword' })(() => ({
  root: {
    width: 'fit-content',
    position: 'relative',
  },
}));

export default useStyles;
