import { makeStyles } from '../../../../theme/makeStyles';

const useStyles = makeStyles({ name: 'LoginFormUI' })(() => ({
  root: {
    width: 'fit-content',
    position: 'relative',
  },
}));

export default useStyles;
