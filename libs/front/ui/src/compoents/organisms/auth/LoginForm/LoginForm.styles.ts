import { makeStyles } from '../../../../theme/makeStyles';

const useStyles = makeStyles({ name: 'LoginForm' })(() => ({
  root: {
    width: 'fit-content',
    position: 'relative',
  }
}));

export default useStyles;
